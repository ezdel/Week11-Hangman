var wordBank = {
	words: ["computer", "hacking", "monitor", "github", "programmer", "bootcamp", "slack", "javascript", "database", "bandwidth", "mainframe", "pseudocode", "motherboard"]
	// var chosen = words[Math.floor(Math.random() * words.length)];
	// chosen = chosen.toUpperCase().split('');
	// length = chosen.length;
	//console.log(chosen);
	//console.log(length);
};
module.exports = wordBank;
